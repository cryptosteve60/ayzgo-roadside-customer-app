
# Frontend Google Maps API Key (RESTRICTED)
# Create a separate API key in Google Cloud Console with these restrictions:
# 1. HTTP referrers: Add your domain (*.lovableproject.com, localhost:*)
# 2. API restrictions: Enable ONLY "Maps JavaScript API" 
# 3. Do NOT enable Geocoding, Directions, or Places APIs (those use backend key)
VITE_GOOGLE_MAPS_API_KEY=
